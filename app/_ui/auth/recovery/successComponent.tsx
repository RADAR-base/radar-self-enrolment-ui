import { Box, Typography } from "@mui/material"

export function SuccessComponent() {
  return <Box>
    <Typography variant="body1">You have successfully entered the recovery code</Typography>
  </Box>
}